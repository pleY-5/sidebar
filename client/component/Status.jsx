import React from 'react';
import styles from '../sidebar.css';

const Status = ({ today, priceRange, healthScore }) => (
  <div className={styles.statusContainer}>
    
  </div>
);

export default Status;